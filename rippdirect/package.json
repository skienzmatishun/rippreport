{
  "name": "rippdirect-worker",
  "version": "1.0.0",
  "description": "RippDirect URL shortener and link preview worker",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production"
  },
  "keywords": ["cloudflare", "worker", "url-shortener", "link-preview"],
  "author": "RippReport",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}